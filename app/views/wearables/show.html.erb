<div data-controller="review">
  <div class="d-flex justify-content-between">
    <div>
      <h1>This is a wearable</h1>
      <h3> title: <%= @wearable.title %> </h3>
      <h5> description: <%= @wearable.description %> </h5>
      <h5> size: <%= @wearable.size %> </h5>
      <h5> brand: <%= @wearable.brand %> </h5>
      <h5> category: <%= @wearable.category %> </h5>
      <h5> price: €<%= @wearable.price %> </h5>

      <div class="d-flex">
        <%= link_to "Book it!", new_wearable_booking_path(@wearable), class: "btn btn-primary m-3"  %>
        <%= link_to "Edit Wearable", edit_wearable_path(@wearable), class: "m-3 d-flex align-items-center" if policy(@wearable).edit? %>
        <%= link_to "Delete", wearable_path(@wearable), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: " d-flex align-items-center text-decoration-underline link-danger m-3" if policy(@wearable).edit? %>
      </div>
    </div>
    <%= render 'carousel' %>
  </div>

  <div id="review_container" data-review-target="box" class="active" data-action="click->review#toggle">
    <h1>Active</h1>

  </div>
</div>
