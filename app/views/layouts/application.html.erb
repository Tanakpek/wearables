<!DOCTYPE html>
<html>
  <head>
    <title>Wearables</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <link href="https://api.mapbox.com/mapbox-gl-js/v2.8.2/mapbox-gl.css" rel="stylesheet">
    <script src="https://api.mapbox.com/mapbox-gl-js/v2.8.2/mapbox-gl.js"></script>

  </head>
  <body>
    <nav class="border-bottom sticky shadow">
      <div class="d-flex justify-content-between p-3">
        <a class="d-flex align-items-center text-decoration-none" href="/">Home</a>
        <a class="d-flex align-items-center text-decoration-none" href="/wearables" >Wearables</a>
        <div class="d-inline-flex">
          <span class="d-flex align-items-center m-2"><%= current_user.email  if user_signed_in? %></span>
            <div class="dropdown">
            <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
              Menu
            </button>
            <ul class="dropdown-menu">
              <li><%= link_to "sign-out", destroy_user_session_path, data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "dropdown-item" %></li>
              <li><a class="dropdown-item" <%= 'href='+"/users/#{current_user.id}"%> href="/users/">My account</a></li>
              <li><a class="dropdown-item" href="#">My bookings</a></li>
            </ul>
          </div>

        </div>
        </div>
      </div>
    </nav>

    <%= yield %>

  </body>
</html>
